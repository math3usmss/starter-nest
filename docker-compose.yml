services:
  nest:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: ${PROJECT_NAME}-nest
    ports:
      - '3001:3000'
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      WATCHPACK_POLLING: 'true'
    restart: unless-stopped
